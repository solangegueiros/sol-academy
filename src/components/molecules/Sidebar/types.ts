export type menu = {
  title: { en: string; es: string; pt: string };
  heading: boolean;
  routes: any;
};

export type submenu = {
  title: { en: string; es: string; pt: string };
  path: string;
};
