import React from 'react';

const B4HPostView = ({ title, description }: any) => {
  return (
    <div>
      <div>${title}</div>
      <div>${description}</div>
    </div>
  );
};

export default B4HPostView;
